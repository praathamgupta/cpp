#include <bits/stdc++.h>
using namespace std;
#define int long long
void solve()
{
    int n, q;
    cin >> n >> q;
    vector<int> house(n);
    for (int i = 0; i < n; i++)
    {
        cin >> house[i];
    }
    vector<int> stores(q);
    for (int i = 0; i < n; i++)
    {
        cin >> stores[i];
    }
}
signed main()
{
    solve();
}