#include <bits/stdc++.h>
using namespace std;
#define int long long
void solve()
{
    int n, m, q;
    cin >> n >> m >> q;
    while (q--)
    {
        int x, y;
        cin >> x >> y;
        if (x == 1)
        {
            int z;
            cin >> z;
        }
        else if (x == 2)
        {
        }
    }
}
signed main()
{
    solve();
}