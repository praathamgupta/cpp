#include <bits/stdc++.h>
using namespace std;
using lli = long long int;
lli dp[101][101];
vector<lli> a;
lli n, k;
lli solve(lli i,lli j){
    
}
void solve()
{
    cin >> n >> k;
    vector<lli> a(n);
    for (lli i = 0; i < n; i++)
    {
        cin >> a[i];
    }
}
int main()
{
    solve();
    return 0;
}