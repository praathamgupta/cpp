#include <bits/stdc++.h>
using namespace std;
using lli = long long;
const lli mod = 10000007;
void solve()
{
    lli n, m;
    cin >> n >> m;
    for (lli i = 0; i < n; i++)
    {
        lli l;
        cin >> l;
        vector<lli> a(l);
        for (lli i = 0; i < l; i++)
        {
            cin >> a[i];
        }
        while (m--)
        {
            lli x;
            cin >> x;
        }
    }
}
int main()
{
    // int t;cin>>t;while(t--)
    solve();
}